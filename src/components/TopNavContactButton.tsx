import {Button} from "@mui/material";

const TopNavContactButton = () => {

    return(
        <Button color="primary" href="#contact">
            Contact
        </Button>
    );
}
export default TopNavContactButton;