import { Button } from "@mui/material";
//import { Link as RouterLink } from "react-router-dom";

const TopNavAboutButton = () => {
    return (
        <Button color="primary" href={"#about"}>
            About
        </Button>
    );
};

export default TopNavAboutButton;

